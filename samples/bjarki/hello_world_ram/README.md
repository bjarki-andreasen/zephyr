To run, build empty app with PM and PM_S2RAM enabled for the application core, then run
the build_nrf54h20dk_nrf54h20_cpurad.sh script, which should build and flash an app to
the radio core, which in this sample tries to mimic bluetooth.
